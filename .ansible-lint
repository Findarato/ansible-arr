---
# .ansible-lint

exclude_paths:
  - .cache/ # implicit unless exclude_paths is defined in config
  - docker_traefik/files/ # implicit unless exclude_paths is defined in config
  - files/
  - vars/main.vault.yml
